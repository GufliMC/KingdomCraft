plugins {
    id 'java'
}

repositories {
    mavenCentral()
}

publishing {
    repositories {
        maven {
            name = "Jorisg"
            url = uri("https://repo.jorisg.com/releases")
            credentials(org.gradle.api.credentials.PasswordCredentials)
        }
    }
}